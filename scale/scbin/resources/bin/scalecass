#!/bin/sh

. `dirname $0`/cassandra.in.sh

if [ -n "$JAVA_HOME" ]; then
    JAVA="$JAVA_HOME/bin/java"
else
    JAVA=java
fi

$JAVA -cp $CLASSPATH edu.uchicago.cs.ucare.cassandra.gms.ScaleSimulator $@ &
