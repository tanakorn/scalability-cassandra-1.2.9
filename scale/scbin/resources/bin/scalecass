#!/bin/sh

. `dirname $0`/cassandra.in.sh

if [ -n "$JAVA_HOME" ]; then
    JAVA="$JAVA_HOME/bin/java"
else
    JAVA=java
fi

echo $JAVA -cp $CLASSPATH edu.uchicago.cs.ucare.cassandra.gms.ScaleSimulator $@ &
